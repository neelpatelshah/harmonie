import { StatusBar } from "expo-status-bar";
import { Text, View } from "react-native";
import Container from "./src/blocks/Container";
import Navigator from "./src/lib/navigator";

const App = () => {
    return <Navigator />;
};

export default App;
